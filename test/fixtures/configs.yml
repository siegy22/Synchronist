sender_payload_path:
  name: sender_payload_path
  value: "<%= Rails.root.join('test/fixtures/data/sender/payload.synchronist') %>"

sender_source_folder:
  name: sender_source_folder
  value: "<%= Rails.root.join('test/fixtures/data/sender/source') %>"

sender_send_folder:
  name: sender_send_folder
  value: "<%= Rails.root.join('test/fixtures/data/sender/send') %>"

receiver_payload_path:
  name: receiver_payload_path
  value: "<%= Rails.root.join('test/fixtures/data/receiver/payload.synchronist') %>"

receiver_storage_folder:
  name: receiver_storage_folder
  value: "<%= Rails.root.join('test/fixtures/data/receiver/storage') %>"

receiver_receive_folder:
  name: receiver_receive_folder
  value: "<%= Rails.root.join('test/fixtures/data/receiver/receive') %>"
